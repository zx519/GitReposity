/**
 * 相同点：
 * 1.单例模式作用就是保证实例仅有一个。
 * 2.私有构造函数，对外提供一个获取实例的方法。
 * 不同点：
 * 1.顾名思义，饿汉式是先创建一个实例，管饱。懒汉式在第一次被引用时实例化一个对象。
 * 2.所以懒汉式节省内存，但饿汉式取实例时更快。
 * 3.饿汉式单例类在类被加载时就将自己实例化，它的优点在于无须考虑多个线程同时访问的问题。
 * 4.饿汉式考虑多线程的问题，要用双重检查锁定等机制进行控制，这将导致系统性能受到一定影响。
 * 程序结果总结：
 * 	由构造函数里的值和输出实例的hashcode看出，真的至创建了一个实例
 */
